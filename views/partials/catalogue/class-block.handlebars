<li class="class-name list-group-item list-group-item-action">{{code}} - <span class="name">{{name}}</span>,
  {{semester}}</li>
<li class="class-content list-group-item">
  {{description}}
  <div class="alert d-none">The class is currently filled. Please check back later.</div>
  <button class="select d-block btn btn-sm btn-primary mt-2" data-class-id="{{id}}" data-class-name="{{name}}"
    data-class-code="{{code}}" data-class-semester="{{semester}}" data-tuition="{{tuition}}" data-available-spaces="{{availableSpaces}}">SELECT</button>
</li>